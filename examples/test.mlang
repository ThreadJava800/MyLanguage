hh fib( c ) {
    if (c <= 0) {
        to_production 0;
    } ;
    if (c == 1) {
        to_production 1;
    } else {
        eto demidovich = c - 1;
        eto java = runmf fib( demidovich );
        
        demidovich = c - 2;
        eto ARIA = runmf fib( demidovich );
        eto po = java + ARIA;

        to_production po;
    } ;
} ;

hh main() {
    eto c = gimme;
    eto linux = runmf fib( c );
    getout linux;
    to_production 0;
} ;

runmf main() ;