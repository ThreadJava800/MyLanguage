hh fib( c ) {
    if (c <= 0) {
        to_production 0;
    } ;
    if (c == 1) {
        to_production 1;
    } else {
        var demidovich = c - 1;
        var java = runmf fib( demidovich );
        
        demidovich = c - 2;
        var ARIA = runmf fib( demidovich );
        var po = java + ARIA;

        to_production po;
    } ;
} ;

hh main() {
    var c = gimme;
    var po = runmf fib( c );
    getout po;
    to_production 0;
} ;

runmf main() ;